<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Se não for um arquivo ou diretório real, redireciona para index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>